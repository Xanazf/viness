# MongoDB store

- Select with BACKEND_STORE=mongo
- Configure connection via MONGO_URI (default: mongodb://localhost:27017/viness)

Example:

```sh
MONGO_URI="mongodb://localhost:27017/viness" BACKEND_STORE=mongo yarn dev:backend
```
